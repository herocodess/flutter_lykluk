name: flutter_job_test
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.0
  video_player: ^2.8.6
  flutter_hooks: ^0.20.5
  http: ^1.2.1
  firebase_core: ^2.31.0
  firebase_storage: ^11.7.5
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
